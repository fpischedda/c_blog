/**
 * layout.h by Francesco Pischedda
 * 
 * layout rendering functions
 */

/**
 * Insert the content in to the layout and returns the result
 *
 * parameters:
 * 	layout: base layout
 * 	content: content to be inserted
 *
 * returns the string after the insertion
 */
char* layout_insert_content(const char* layout, const char* content);